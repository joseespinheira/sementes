
<nav class="navbar navbar-expand-lg navbar-light bg-light" style="background-color: #4CAF50;border-radius: 10px;">
  <a class="navbar-brand" href="/">SEMENTES</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
    <div class="navbar-nav">
      <div class="collapse navbar-collapse">
        <p></p>
        <% if usuario_signed_in? %>
          <a class="nav-item nav-link" href="/">Inicio</a>
          <% if ( current_usuario.admin == true) %>
            <a class="nav-item nav-link" href="/plantas">Plantas</a>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>
      <div class="navbar-nav" style="float:right">
        <div class="collapse navbar-collapse">
          <% if usuario_signed_in? %>
            <a class="nav-item nav-link">Logado com: <strong><%= current_usuario.email %></strong>  </a>
            <%= link_to 'Editar prefil', edit_usuario_path(current_usuario), :class => 'nav-item nav-link' %>
            <%= link_to "Sair", destroy_usuario_session_path, method: :delete, :class => 'nav-item nav-link',:style => "color:red;"  %>
          <% else %>
            <%= link_to "Cadastrar", new_usuario_registration_path, :class => 'nav-item nav-link'  %>
            <%= link_to "Entrar", new_usuario_session_path, :class => 'nav-item nav-link'  %>
          <% end %>
        </div>

    </div>

</nav>
