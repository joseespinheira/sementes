<%= form_for(planta) do |f| %>
  <% if planta.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planta.errors.count, "error") %> prohibited this planta from being saved:</h2>

      <ul>
      <% planta.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nome %>
    <%= f.text_field :nome %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
